#!/usr/bin/env bash

# Runs WDS

export NODE_ENV='development'
export PORT=1337
export PUBLIC_PATH=''
export HOST='localhost'

export NODE_OPTIONS="--no-deprecation"

echo -en "\033]0;RocketBroom » WDS « ${NODE_ENV}\a"

node --harmony `which webpack-dev-server`
