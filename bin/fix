#!/usr/bin/env bash

# Runs ESLint and fixes it's errors if possible.

# FIXME: OH, NO, `stylelint --fix` on JS files breaks all.
# TODO: Definitely, we need `eslint-stylelint-plugin`. Also, with it stylelint for styled-components may be seamlessly integrated into IDEA.

eslint --fix "." "./src/**/*.{js,jsx}"
